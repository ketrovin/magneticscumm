const fs = require('fs');

function decodeImage() {
    // Actually, we need to inspect the pixels to find bounding boxes.
    // Instead of doing it from scratch, I'll write a simple BMP extraction if I convert it to BMP.
}
